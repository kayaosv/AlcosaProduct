// Umbral global de stock bajo para el dashboard de admin. Un solo número
// a propósito (no columna por producto) — si hace falta afinar por
// producto más adelante, se añade una columna entonces.
export const LOW_STOCK_THRESHOLD = 5
